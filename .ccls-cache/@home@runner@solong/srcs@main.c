#include "so_long.h"

// int	ft_init_game(t_game *game, char *map_path)
// {
// 	if (ft_parse_map(game, map_path) == -1)
// 		return (-1);
// 	return (0);
// }

int main(int argc, char **argv)
{
  (void) argc, (void) argv;
	// t_game game;

	// if (argc != 2)
	// 	return (0);
	// if (ft_init_game(&game, argv[1]) == -1)
	// 	return (0);
	// char **map = parse_map(argv[1]);
	// (void)map;
	// game.mlx = mlx_init();
	// game.win = mlx_new_window(game.mlx, 1000, 1000, "fdf");

	// mlx_loop(game.mlx);
	return (0);
}